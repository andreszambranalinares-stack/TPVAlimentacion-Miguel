# Perfil de tests: H2 en memoria en modo compatibilidad PostgreSQL
spring:
  datasource:
    url: jdbc:h2:mem:tpv;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password: ""
    driver-class-name: org.h2.Driver
  flyway:
    locations: classpath:db/migration
